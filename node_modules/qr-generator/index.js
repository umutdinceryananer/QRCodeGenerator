const qr = require('qrcode-terminal');

const generate = (url) => {

  if (!url) {
    throw new Error('qr-generator requires a URL String as an argument');
  }

  qr.generate(url);

};

module.exports = generate;